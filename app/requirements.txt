# ====================================================================
# VisionAssistLFAI - Consolidated System Dependency Manifest
# Installation: pip install -r requirements.txt
# ====================================================================

# 🖥️ Core Frontend Framework
streamlit==1.42.0

# 🎙️ Speech Processing & Native Audio Drivers
SpeechRecognition==3.10.4
pyaudio==0.2.14; sys_platform == 'win32' or sys_platform == 'darwin'
pyttsx3==2.90

# 👁️ Headless Edge Computer Vision
ultralytics==8.4.106
# opencv-python-headless==4.10.0.84

# YOLOE's set_classes() needs a CLIP-style text encoder. Pinned here explicitly
# so it's baked into the image at build time — without this, Ultralytics
# silently pip-installs it at RUNTIME on first YOLOE use (visible as
# "Successfully installed clip-... ftfy-... wcwidth-..." in container logs),
# live inside the already-running Streamlit process. That live install doesn't
# take effect without restarting the interpreter (Ultralytics prints exactly
# this warning), and continuing to run with the mismatched state caused a real
# SIGSEGV crash (confirmed via docker inspect ExitCode after a photo scan).
clip==1.0
ftfy==6.3.1
wcwidth==0.8.2

# 🧠 LLM Orchestration, Routing & Agent Primitives
openai==1.61.0
langchain
langchain-openai
ollama==0.6.2
# openai-whisper
# torch==2.7.1

# 🔎 Vector Database Client
qdrant-client==1.11.0

# 🎙️ Local Speech Processing Core
faster-whisper==1.0.3
gTTS==2.5.1  

# 🧪 Testing
pytest==8.3.4
pytest-cov==6.0.0

# ⚙️ Infrastructure & Communication Utility Layers
python-dotenv==1.0.1
requests==2.32.3
pandas==2.2.1
numpy==1.26.4
# ffmpeg-python

# 🗄️ Database Adapter
psycopg2-binary==2.9.9